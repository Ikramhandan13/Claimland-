action-bar-enabled: true
action-bar-format: "Land: %land_name% | Owner: %land_owner%"
tax:
  weekly-amount: 100.0
  grace-days: 7
  auto-unclaim-if-unpaid: true
database:
  type: sqlite # sqlite or mysql
  sqlite-file: data.db
  mysql:
    host: localhost
    port: 3306
    database: claimland
    user: root
    password: password
scheduling:
  use-folia-safe: true
gui:
  page-size: 45
debug: false
